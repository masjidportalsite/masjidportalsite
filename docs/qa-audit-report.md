# QA Audit Report: Lint and Code Quality Issues

## Executive Summary
A comprehensive audit of the MasjidPortalSite codebase has been performed using `npm run lint` and manual inspection. While many previous issues have been partially addressed, several critical code quality issues remain, primarily hidden by `eslint-disable` comments.

## Statistical Overview
- **Critical Errors (Blocking Quality):** 3
- **Warnings (Non-blocking):** 5
- **Accessibility Issues Identified:** 2
- **Hidden Issues (Bypassed via lint-disable):** 6+

---

## Detailed Findings

### 1. Critical Errors (Code Quality & Performance)
These issues are marked as `error` by the current ESLint configuration and can cause performance degradation or runtime instabilities.

| Location | Issue | Severity | Status |
|----------|-------|----------|--------|
| `src/app/dashboard/layout.tsx` | `setState` called synchronously within `useEffect` | Critical | **BYPASSED** |
| `src/components/dashboard/settings/branding-settings.tsx` | `setState` called synchronously within `useEffect` | Critical | **BYPASSED** |
| `src/components/providers/branding-provider.tsx` | `setState` called synchronously within `useEffect` | Critical | **BYPASSED** |

*Note: Synchronous state updates in effects can trigger cascading renders and hurt performance.*

### 2. Warnings (Maintenance & Best Practices)
These issues should be addressed to ensure long-term maintainability and adherence to Next.js best practices.

| Location | Issue | Severity | Status |
|----------|-------|----------|--------|
| `src/app/layout.tsx` | Google Font `display=block` not recommended | Warning | Open |
| `src/app/layout.tsx` | Custom fonts not added via `next/font` | Warning | Open |
| `src/components/providers/branding-provider.tsx` | Missing dependency `fetchSettings` in `useEffect` | Warning | Open |
| `src/app/dashboard/layout.tsx` | Missing dependency `rolesList` in `useEffect` | Warning | Open |
| `src/components/dashboard/settings/branding-settings.tsx` | Unused variable `refreshSettings` | Warning | **BYPASSED** |

### 3. Accessibility & Optimization Issues
| Location | Issue | Severity | Status |
|----------|-------|----------|--------|
| `src/components/dashboard/settings/branding-settings.tsx` | Use of `<img>` instead of `next/image` | Medium | **BYPASSED** |
| Multiple Files | Unescaped entities in JSX (e.g., `'` should be `&apos;`) | Low | Partially Fixed |

---

## Technical Debt: Lint Bypass Analysis
The following files have explicit `eslint-disable` or `eslint-disable-next-line` comments that hide real issues:

1. **`src/app/dashboard/layout.tsx`**: Disables `react-hooks/set-state-in-effect` and `react-hooks/exhaustive-deps`.
2. **`src/components/dashboard/settings/branding-settings.tsx`**: Disables `react-hooks/set-state-in-effect`, `@typescript-eslint/no-unused-vars`, and `@next/next/no-img-element`.
3. **`src/components/providers/branding-provider.tsx`**: Disables `react-hooks/set-state-in-effect`.
4. **`src/app/dashboard/members/page.tsx`**: Disables `@typescript-eslint/no-unused-vars` for `getRoleBadge`.

---

## Recommendations
1. **Remove Lint Bypasses:** All `eslint-disable` comments should be removed and the underlying issues fixed properly.
2. **Refactor Effects:** Use `useCallback` for functions called in `useEffect` and ensure all dependencies are correctly listed.
3. **Adopt `next/font`:** Migration to `next/font/google` in `layout.tsx` will resolve font-related warnings and improve performance.
4. **Image Optimization:** Replace all standard `<img>` tags with Next.js `<Image />` component for better LCP and bandwidth optimization.
5. **Type Safety:** Continue replacing `any` with `unknown` or proper interfaces (good progress already made here).

## Build Status
- **Last Build Attempt:** Success (with warnings)
- **Environment:** Production-like build with Turbopack

---
*Report generated by agent-qa-agent on 2026-05-18*
